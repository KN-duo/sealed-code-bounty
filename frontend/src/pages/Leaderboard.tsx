import { EmptyState } from "../components/ui/states";

// Placeholder — implemented in a later milestone.
export function Leaderboard() {
  return <EmptyState title="Leaderboard" message="Under construction." />;
}
