import { EmptyState } from "../components/ui/states";

// Placeholder — implemented in M5.
export function SubmitConsole({ pda }: { pda: string }) {
  return <EmptyState title="Submit console" message={`Under construction for ${pda}.`} />;
}
